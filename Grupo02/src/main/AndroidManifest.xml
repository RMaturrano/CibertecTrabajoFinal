<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cibertec.pe.proyectofinal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Primera_Actividad_Splash"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".Tercera_Actividad_Principal_Clientes" />

        <activity android:name=".Segunda_Actividad_Login" />

        <activity android:name=".ClienteConsultaActivity" />
        <activity android:name=".ClienteEditarActivity" />
        <activity android:name=".ClienteMapaActivity" />
        <activity android:name=".ClienteNuevoActivity" />
        <activity android:name=".PrincipalClienteActivity" />

    </application>









</manifest>